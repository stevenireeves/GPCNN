#include "interp.h" 

GPinterp::GPinterp(std::vector<float> wts, std::vector<float> gam){
	weights = wts; 
	gamma = gam; 
}

GPinterp::interp(const std::vector<const float> img_in, std::vector<float> img_out){


}

